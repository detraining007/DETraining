class recursion(object):
    def factorial(self,x):
        if (x==0)
         return 1
        else:
           return x*self.factorial(x-1)
    def recursion()
       print(input("enter the number of factorial"))
